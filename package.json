{
  "name": "primary-math-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "test": "vitest"
  },
  "dependencies": {
    "pinia": "^2.0.28",
    "vue": "^3.2.41",
    "vue-router": "^4.1.6"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^4.9.5",
    "vite": "^6.0.0",
    "vitest": "^0.28.1",
    "vue-tsc": "^1.0.9",
    "wrangler": "^3.0.0"
  }
}
